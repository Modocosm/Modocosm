<section class="nopad navigation-filler"></section>
<section class="nopad hero">
	<mc-container>
		<mc-hero>
			<img class = "left float" src ="phone 600.png"/>
			<img class = "center float desktop" src="laptop2 1200.png" />
			<img class = "right float" src="Moto@2x.png"/>
			<div class = "content">
				<h1>We Build Stellar Brands.</h1>
				<p>Modocosm helps brands and business take off.</p>
			</div>
		</mc-hero>
	</mc-container>
</section>
<style>
    :root {
					--float-shadow:drop-shadow(1px 1px 3px var(--color-body-muted));
					--float-shadow-alt:drop-shadow(2px 5px 8px var(--color-body-muted));
				}
				html,body {
					max-width:100%;
				}
				mc-container {
					display:block;
				}
				mc-hero {
					position:relative;
					height:60vh;
					min-height: 100vh;
					display: inline-block;
					width: 100%;
					overflow: hidden;
					padding-bottom:50px;
				}
				mc-hero .content {
					position:absolute;
					bottom:-36px;
					width:100%;
					text-align: Center;
				}
				mc-hero img.float {
					position:absolute;
					min-width:unset;
					filter:var(--float-shadow);
					animation:float 6s ease-in-out infinite;
					animation-direction: alternate;
				}
				mc-hero img.float.left {
					left:15%;
					width:20%;
					max-width:20%;
					top:30%;
					transform:rotate(15deg);
					animation-name:float3;
				}
				mc-hero img.float.center {
					width:60%;
					max-width:60%;
					left:30%;
					top:-5%;
				}
				mc-hero img.float.right {
					right:15%;
					top:25%;
					transform:rotate(-35deg);
					animation-name:float2;
				}
				@keyframes float {
					0% {
						filter:var(--float-shadow);
						transform: translatey(0px);
					}
					50% {
						filter:var(--float-shadow-alt);
						transform: translatey(-20px);
					}
					100% {
						filter:var(--float-shadow);
						transform: translatey(0px);
					}
				}
				@keyframes float2 {
					0% {
						filter:var(--float-shadow);
						transform: translatey(0px) rotate(-35deg);
					}
					50% {
						filter:var(--float-shadow-alt);
						transform: translatey(-20px) rotate(-25deg);
					}
					100% {
						filter:var(--float-shadow);
						transform: translatey(0px) rotate(-35deg);
					}
				}
				@keyframes float3 {
					0% {
						filter:var(--float-shadow);
						transform: translatey(0px) rotate(15deg);
					}
					50% {
						filter:var(--float-shadow-alt);
						transform: translatey(-20px) rotate(20deg);
					}
					100% {
						filter:var(--float-shadow);
						transform: translatey(0px) rotate(15deg);
					}
				}
				@keyframes float4 {
					0% {

						transform: translatey(0px) rotate(15deg);
					}
					50% {

						transform: translatey(-20px) rotate(20deg);
					}
					100% {

						transform: translatey(0px) rotate(15deg);
					}
				}
</style>